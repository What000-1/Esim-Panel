# Esim-Panel 改进审查报告

> 本文保留原审查基线。报告中的问题已在后续修复，详见 [修复报告](2026-09-14_fix-Esim-Panel-report.md)；历史复现脚本固定读取 Git 提交 `2fe0864`，新版回归检查使用 `npm test`。

审查日期：2026-09-14。基线：`2fe0864`。范围见 [scope.md](work/review-2026-09-14/scope.md)。

项目的主要风险集中在数据一致性、输入校验和提醒可靠性。两个并发新增请求均返回成功时，本地模拟只保留了一条记录；异常导入也能令整个定时提醒中断。前端存在未经转义的 HTML 插入路径，且若干常用操作会留下错误状态。建议先解决这些正确性问题，再拆分单文件与改善体验。本次未修改应用源代码或部署，所有发现均为待修复。

## 优先改进项

P1 表示优先修复，P2 表示随后修复，P3 表示低优先级。各项均有明确代码依据；“本地确认”指替身环境复现，不代表线上事故已发生。

| ID | 优先级 | 发现与位置（worker/worker.js） | 证据 | 置信度 / 状态 |
|---|---|---|---|---|
| F01 | P1 | 整表读改写丢失并发更新；1890、1914–1915、1942、2026 | E04 | 高 / 本地确认 |
| F02 | P1 | 续期弹窗名称未经转义，存在存储型 XSS 路径；1303 | E02 | 高 / 数据流确认，未做浏览器执行验证 |
| F03 | P1 | 导入及写入缺少统一字段校验；1778–1805、1903、1928、1844 | E03 | 高 / 本地确认 |
| F04 | P1 | TG 提醒合并过长且不检查发送结果；2029–2040 | E06、官方文档 | 高 / 本地确认及文档核对 |
| F05 | P2 | 一个被限流的 IP 能删除全局验证码；1736–1739 | E07 | 高 / 本地确认 |
| F06 | P2 | 月末、闰日续期溢出目标月份；693–705、1626–1637 | E05 | 高 / 计算结果确认，业务规则待明确 |
| F07 | P2 | 登录成功后按钮未恢复，退出后无法再次点击登录；775–801、804–808 | E09 | 高 / 本地确认 |
| F08 | P2 | 搜索改变总览统计，空列表保留旧统计；1061–1075、1083、1201 | E08、源码 | 高 / 本地确认 |
| F09 | P2 | “添加时间”排序实际按随机 UUID 排序；867–869、1910 | E10 | 高 / 本地确认 |
| F10 | P2 | CORS 用字符串包含判断来源，允许非预期域；1657–1658 | E11 | 高 / 本地确认，仍需 Bearer Token |

### F01：保存成功但修改丢失

每次新增、编辑、删除、导入和自动延期都先读取 `esim_list`，再把整个数组覆盖写回。两个标签页同时修改，或页面修改与 Cron 重叠，会基于同一旧版本分别写回，最后一次覆盖前一次。E04 强制两个新增请求读到相同快照，两次均返回 HTTP 200，最终只有一条。

建议把所有修改交由同一 Durable Object 串行处理并使用其事务存储；或者使用支持事务的数据库按卡片更新，并加入版本冲突检测。单纯在现有 KV 中增加版本字段再读写，不能提供原子比较与更新保证。Cloudflare 官方说明 KV 采用最终一致性，不适用于原子操作或单事务读写，并建议此类需求考虑 Durable Objects。[Cloudflare KV 一致性说明](https://developers.cloudflare.com/kv/concepts/how-kv-works/)

### F02：名称进入可执行 HTML 上下文

创建和编辑允许名称包含 HTML，卡片列表对名称转义，但 `openRenewModal()` 将 `sim.name` 直接插入 `innerHTML`。E02 的无害名称 `<b data-review="marker">Example</b>` 原样到达此位置。含事件属性的标签因而存在脚本执行风险，执行后可访问当前源的会话存储和管理 API。

前提是攻击者控制被录入或导入的数据，且用户打开对应续期弹窗；这不是已证实的未登录远程接管。应使用 `textContent` 或创建文本节点。导入的 `id` 还会进入行内事件处理器，`expireDate` 等字段也直接插入卡片 HTML，应同时校验字段并移除字符串拼接的行内事件。

### F03：坏数据会破坏面板和全部定时任务

导入只检查最外层是否为数组，没有检查元素。E03 通过已登录的模拟会话导入 `[null]`，接口返回 200，下一次 Cron 抛出 `Cannot read properties of null (reading 'expireDate')`。错误号码类型、非法日期、异常周期也可能进入存储。`POST`、`PUT`、批量续期各自校验不同，修补单一路由无法闭环。

建议共享一套 schema，检查非空对象、ID 唯一性、字符串长度、真实日历日期、正整数周期、周期单位枚举、布尔值和提醒天数边界。导入应整批验证完成后才写入，返回具体错误行；Cron 对历史坏记录逐条隔离并记录错误，避免一张卡中断所有提醒。覆盖导入前显示将替换的记录数量。

### F04：提醒可能整批丢失，任务却显示成功

代码把当天全部提醒拼成一条消息。E06 用 10 张卡、每张 500 字备注生成 6133 个 JavaScript 字符长度的消息；仅备注已达 5000 字，去除格式标记后仍超限。模拟 TG 返回 HTTP 400 时，`scheduled()` 仍正常结束。Telegram `sendMessage` 限制解析后的文本为 1–4096 字符。[Telegram sendMessage 文档](https://core.telegram.org/bots/api#sendmessage)

建议按记录分批发送，超长单条也单独处理，避免切断 HTML 标签或实体。检查 HTTP 状态及 JSON `ok`，记录失败并有限重试；对重试添加通知去重标识。自动延期在发送前已写库，需保留待发送状态，否则失败后下一次可能不再触发同一条通知。

### F05：OTP 限流会影响其他人的正常登录

错次按 IP 记录，但超限分支删除共享的 `admin_auth_code`。因此某个 IP 累积五次错误后，只要再次提交验证，即可让其他 IP 刚获取的验证码失效；该 IP 的计数仍在过期前保留。E07 预置五次错误计数，返回 403 的同时共享验证码被删除。

建议让验证请求携带独立挑战 ID，错次、过期和消费状态绑定到挑战；被限流的请求不能删除别人的挑战。OTP 单次消费和限流计数需要原子状态管理，避免 KV 读改写竞态；发送接口也应避免任意新请求覆盖其他有效挑战。

### F06：月末续期不符合常见的自然月规则

当前直接调用 `setMonth` / `setUTCMonth`，结果为 `2026-01-31 + 1 月 = 2026-03-03`；`2024-02-29 + 1 年 = 2025-03-01`。如果运营商约定到目标月份最后一天到期，这会把看板日期延后并推迟提醒。

建议先明确自然月/月末规则，再将目标日期钳制到目标月末，前后端共享计算实现；覆盖 1 月 31 日、闰日和季度跨年。另有日期解析不一致：展示使用 `new Date('YYYY-MM-DD')` 后转本地午夜，续期使用本地年月日构造；负时区用户可能显示前一天。应统一业务时区和日期专用表示。

### F07–F10：常用界面的正确性问题

- F07：`verifyCode()` 开头禁用按钮，仅失败分支恢复。登录成功再退出后，按钮仍处于禁用及“验证中”状态。用 `finally` 恢复按钮，退出时统一清理弹窗、批量栏、内存数据与 UI 状态。
- F08：统计计数来自过滤后的列表，与“基于全部数据”的注释不符；空列表提前返回，删除最后一条后仍显示旧计数。统计与卡片渲染分离，始终从 `esimData` 计算，空集合写入零。
- F09：UUID 不携带添加时间，按 ID 字典序不能表示新旧。新增显式 `createdAt` 字段，旧数据可用已有时间戳 ID 或保持相对顺序，不应伪造历史时间。
- F10：`https://workers.dev.attacker.invalid` 被反射为允许来源，任意其他 `workers.dev` 租户也被允许。只接受当前源或显式配置的完整源。当前 API 仍需要令牌，CORS 宽松本身不等于无凭据读取数据。

## 后续维护与体验改进

- 把 2043 行文件分为 HTML/样式、前端逻辑、API、认证、日期工具和定时任务；仍可构建成一个 Worker 部署产物。
- 建立最小回归检查，优先覆盖上述数据丢失、导入、提醒和日期边界；当前仓库没有测试配置或依赖锁文件。
- 备份菜单仅依赖 hover，建议增加点击开关、键盘操作、焦点管理和语义标签。触屏行为尚未实机验证。
- 剩余时间进度条固定除以 365，建议根据该卡实际周期计算。
- README 仍描述时间戳 ID、仅天数周期及仅“基于今天”续期，与当前 UUID、多单位周期和双模式续期不一致；“全局转义”“抵御任何形式 XSS”等绝对安全表述应调整为可验证的具体能力。

## Evidence → Finding → Path

可执行证据脚本：[checks.cjs](work/review-2026-09-14/checks.cjs)。输出：[results.json](work/review-2026-09-14/results.json)。

工具为本机 Node.js v24.18.1，使用 `vm`、内存 KV、最小 DOM 替身和模拟 HTTP 响应。源码 SHA-256：`4edcbc630bcfcfc6ce4fd3bdc554acb188a2af72e05751ec45910233bbee1ebb`。E01 为 Worker 和生成的内联脚本语法检查；E02–E11 与发现表一一映射。证据 source_ref 为上述脚本和输出，content_hash 使用基线源码哈希。脚本断言的是“问题存在”，全部成功不代表应用通过正确性测试。

在项目根目录复现（仅本地）：

```powershell
node --check worker/worker.js
node work/review-2026-09-14/checks.cjs
```

| Path ID | 类型 | 调用路径与影响 |
|---|---|---|
| P01 | callflow | 两个 POST → 读取相同 esim_list → 各自追加 → 整表覆盖 → E04 / F01 丢失记录 |
| P02 | callflow | 含标签名称 → 保存或导入 → openRenewModal → innerHTML → E02 / F02 HTML 注入路径 |
| P03 | callflow | 已登录导入 [null] → 存储成功 → scheduled 遍历 → E03 / F03 全任务异常 |
| P04 | callflow | 多张卡到期 → 合并消息 → 模拟 HTTP 400 → 未检查响应 → E06 / F04 静默失败 |

时间线：本轮先读取仓库与部署配置；随后人工跟踪数据写入、认证和渲染边界；再核对官方服务约束并运行本地复现；最后保存本报告。均发生于 2026-09-14。

限制：未进行真实浏览器脚本执行或视觉 QA；未连接生产 Worker、KV 或 Telegram。Semgrep 本机未发现，未运行该扫描器，以定向源码检索和人工验证替代。技能的共享 tool-index、授权先例和 Evidence 契约文件在指定位置缺失，授权采用本轮用户请求，证据结构采用已读取的报告模板。不声称完成线上渗透测试或全面安全认证。

完成检查：已限定授权仓库；已验证关键发现；已给出修复措施和复现命令；已记录限制；未修改业务代码，未部署，未发送外部消息。
