# Esim-Panel 修复与验证报告

日期：2026-09-14。范围与授权：[scope.md](work/review-2026-09-14/scope.md)。原始问题：[审查报告](2026-09-14_code-review-Esim-Panel-report.md)。

最终复核：2026-09-17，`npm test` 的 23 项测试全部通过；Wrangler 部署预演通过，构建包为 369.96 KiB（gzip 75.12 KiB）；`git diff --check` 通过。未执行实际部署。

报告中的 F01–F10 已修复，并完成模块拆分、依赖锁定、CI、菜单与弹窗改进、进度条修正及部署文档更新。卡片存储从 KV 的整表读改写迁移到 SQLite Durable Object，旧数据只读迁移、异常记录隔离、原 KV 保留。验证包含本地数据库/DOM 回归、真实 workerd 运行时与桌面/手机宽度浏览器操作。本次没有上传或部署 Worker，也没有访问生产卡片或发送真实 Telegram 消息。

## 修复对应关系

| 发现                | 改动位置                                             | 修复与回归覆盖                                                                                                         | 状态   |
| ------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------ |
| F01 并发覆盖        | `worker/store.js`、`web/app.js`                      | 固定 DO、串行队列与 SQLite 事务；独立新增保留所有记录；旧版本编辑/删除/导入/批量操作返回 409；弹窗保存打开时的快照版本 | 已修复 |
| F02 HTML 注入       | `web/app.js`、`scripts/build.mjs`、`worker/entry.js` | 续期名称使用 textContent；动态字段转义；卡片操作采用事件委托；构建移除静态行内处理器，script CSP 限定同源              | 已修复 |
| F03 异常数据        | `worker/validation.js`、`worker/store.js`            | 所有写入共享 schema；导入整批验证、重复 ID 检测、错误行号；旧坏记录隔离；KV 读取失败不初始化空库                       | 已修复 |
| F04 提醒丢失        | `worker/telegram.js`、`worker/store.js`              | 每条提醒持久化，纯文本分段、检查 HTTP 和 ok、Alarm 退避重试；同日去重；失败状态可见并能手动重新入队                    | 已修复 |
| F05 全局 OTP 被删除 | `worker/store.js`                                    | 独立挑战与 IP 关联、每挑战五次、IP 频控、单次消费；超限不影响其他挑战，发送失败不创建可用挑战                          | 已修复 |
| F06 日期边界        | `shared/dates.js`                                    | 统一北京时间的日期值，目标月末钳制；月、季度、年、闰日和日历日期合法性测试                                             | 已修复 |
| F07 登录按钮        | `web/app.js`                                         | finally 恢复按钮；退出注销服务器会话并清理 DOM、批量栏、弹窗和内存数据                                                 | 已修复 |
| F08 总览统计        | `web/app.js`                                         | 独立从全部卡片计算统计；搜索无结果不改总览，删除最后记录显示全零                                                       | 已修复 |
| F09 添加时间排序    | `worker/validation.js`、`web/app.js`                 | createdAt 由服务端生成；恢复旧时间戳 ID；未知时间放最后，不伪造 UUID 历史                                              | 已修复 |
| F10 来源校验        | `worker/entry.js`                                    | 精确同源，拒绝其他租户和包含关键词的伪装来源；外部不能调用内部 Cron 路径                                               | 已修复 |

另外，备份菜单支持点击与 Escape，弹窗限制焦点范围并使背景不可交互；批量栏关闭时真正隐藏；剩余进度使用实际周期。旧版 AES 加密备份继续兼容，覆盖导入会确认记录数量。Tailwind 在构建时生成 CSS；备份库随 Worker 提供，图标字体和国旗图片仍使用第三方 CDN。

## Evidence

| E-id     | source_ref                                                                 | repro_command                                                   | content_hash                                       |
| -------- | -------------------------------------------------------------------------- | --------------------------------------------------------------- | -------------------------------------------------- |
| E-FIX-01 | `tests/core.test.mjs`、`tests/frontend.test.mjs`、`tests/runtime.test.mjs` | `npm test`                                                      | n/a：活跃工作区测试，依赖固定于 package-lock.json  |
| E-FIX-02 | `wrangler.toml`、`worker/entry.js`、构建产物                               | `npx wrangler deploy --dry-run --outdir .wrangler/dry-run`      | n/a：构建产物随源码重新生成                        |
| E-FIX-03 | npm 安装审计结果                                                           | `npm audit`                                                     | n/a：官方漏洞数据库会更新；本次安装审计为 0 个漏洞 |
| E-FIX-04 | 本地浏览器操作，样例服务 `work/review-2026-09-14/preview.mjs`              | `npm run build`，然后 `node work/review-2026-09-14/preview.mjs` | n/a：浏览器现场观察                                |

E-FIX-01 包含 23 项测试，使用内存 SQLite、jsdom 和 Miniflare/workerd。测试固定命中错误输入、竞态、失败响应等分支，输出中的模拟错误日志是预期证据。E-FIX-04 在 1280 像素桌面视口及 390×844 手机视口验证了验证码登录、点击备份菜单、Escape 关闭、续期弹窗与保存后统计变化。此样例服务只监听回环地址，所有数据和验证码均为本地测试用途。

## Path

| P-id     | path_type | 修复后的调用链                                                                         | findings / evidence              |
| -------- | --------- | -------------------------------------------------------------------------------------- | -------------------------------- |
| P-FIX-01 | callflow  | API → 精确来源与会话验证 → 固定 DO 队列 → schema / If-Match → SQLite 事务 → 新版本响应 | F01/F03/F10；E-FIX-01            |
| P-FIX-02 | callflow  | API 数据 → 字段转义 / textContent → 委托事件 → 续期表单 → 共享日历规则与服务端验证     | F02/F06/F07/F08/F09；E-FIX-01/04 |
| P-FIX-03 | callflow  | Cron → 写入 Alarm → 日期与通知事务 → Alarm 投递 → 确认成功或退避 → 失败可见/手动重试   | F04；E-FIX-01                    |
| P-FIX-04 | callflow  | 发送限流 → TG 确认成功 → 独立挑战 → 错次隔离 / 原子消费 → 会话 → 服务端注销            | F05/F07；E-FIX-01                |

## 上线要求与限制

升级前，需按 [README](README.md#从旧版升级) 在 `wrangler.toml` 中声明原 `ESIM_DB` namespace，并保留 Durable Object 迁移配置。缺少原绑定时初始化会停止；全新安装才设置 `FRESH_INSTALL=true`。本次没有真实 namespace ID，所以没有替用户填写或部署。

通知有持久化与重试，但 Telegram 发送成功后若进程在确认入库前中断，或网络结果不明，仍可能重复投递；不能承诺外部消息严格只发送一次。图标/国旗 CDN 可用性、真实 TG 接收、生产 KV 迁移和云端配额需要上线环境核对。旧版恢复也需手动同步新版期间的修改，原 KV 不会自动更新。

时间线：本轮先拆分源码并增加存储/校验模块；随后修复前端与通知认证流程；运行本地回归和 workerd 集成测试；完成桌面与手机宽度检查；最后更新迁移说明、CI 和修复报告。各项置信度为高，依据为代码数据流和对应测试；没有宣称全面安全认证。

完成清单：授权范围内修改；F01–F10 逐项覆盖；旧数据迁移与失败路径验证；复现命令与上线前置条件记录；无生产写入或外部消息。
