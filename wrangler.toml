# 这个名字决定了你最终的 API 链接前缀，比如 https://esim-api.你的用户名.workers.dev
name = "esim-api"

# 明确告诉 Cloudflare 我们后端代码的准确路径
main = "worker/worker.js"

# 更新为最新的兼容性日期，满足 Cloudflare 新版要求
compatibility_date = "2026-05-31"

# 新版 Cloudflare (Wrangler v3.109.0+) 强烈建议添加的日志观测配置，消除构建警告
[observability]
enabled = true

# 自动配置定时任务（每天 UTC 02:00，北京时间 10:00）
[triggers]
crons = ["0 2 * * *"]

# 提示：由于安全防范，请勿在此处硬编码 KV ID，防止通过公开仓库泄漏。
# 请在 Cloudflare 控制台 Worker 详情页的 Settings -> Variables -> KV Namespace Bindings 中手动绑定变量名 ESIM_DB
# [[kv_namespaces]]
# binding = "ESIM_DB"
# id = "YOUR_KV_NAMESPACE_ID"
